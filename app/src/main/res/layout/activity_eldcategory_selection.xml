<layout>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/viewer_blank"
        android:transitionGroup="false"
        android:orientation="vertical"
        tools:ignore="Overdraw,UnusedAttribute">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar_viewer"
            android:layout_width="match_parent"
            android:layout_height="?android:attr/actionBarSize"
            style="@style/ELDViewer.TextAppearance.Title"
            android:background="@color/viewer_primary"
            android:elevation="@dimen/elevation_header"
            tools:ignore="UnusedAttribute">
        </android.support.v7.widget.Toolbar>

        <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/category_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:context=".eldfileviewer.activity.ELDCategorySelectionActivity"
            android:background="@color/light_grey"
            android:transitionGroup="false"
            tools:ignore="MergeRootFrame,Overdraw,UnusedAttribute">

            <ProgressBar android:id="@+id/progress"
                android:layout_width="@dimen/size_fab"
                android:layout_height="@dimen/size_fab"
                android:layout_gravity="center"
                android:indeterminateOnly="true" />
        </FrameLayout>

    </LinearLayout>
</layout>